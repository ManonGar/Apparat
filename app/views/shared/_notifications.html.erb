<div class="container">

    <% current_user.bookings.each do |booking| %>
      <% if booking.validation %>
          <div class="notification">
            <%= booking.costume.user.photos.first.key%>
            <%= booking.costume.user.first_name %> 
          </div>
          <div class="notification-content">
            <p> <%= booking.costume.user.first_name %> a réservé votre costume <%=booking.costume.name%> du <%=booking.beginning_date%> au <%=booking.ending_date%>.
            </p>
          </div> 
      <% end %>
    <% end %> 
  
    <% current_user.bookings.each do |booking| %>
      <% if booking.validation == false %>
          <div class="notification">
            <%= booking.costume.user.photos.first.key%>
            <div class="notification-content">
              <p> <small> <%= booking.costume.user.first_name %> </small></p>
              <p> <%= booking.costume.user.first_name %> aimerait louer votre <%=booking.costume.name%> du <%=booking.beginning_date%> au <%=booking.ending_date%>.
              </p>
              <p>Souhaitez-vous accepter ou refuser sa demande?</p>
              </div> 
            <div class="notification-actions">
              <%= link_to "✔️", edit_booking_path(booking), class: "btn btn-ghost" %> 
              <%= link_to "❌", booking_path(booking), method: :delete, data: {confirm: "Etes-vous sûr de vouloir supprimer cette réservation?"}, class: "btn btn-ghost" %>
            </div>
          </div>
     <% end %>
  <% end %>

</div>