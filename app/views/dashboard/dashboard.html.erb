<div class="container">
  <h3> Bienvenue <%= current_user.email %> 👋</h3>
  <p>N'hésites surtout pas à naviguer sur les différents onglets ci-dessous</p>
  <ol>
    <li><strong>Mes costumes:</strong> l'ensemble des costumes que tu as pu mettre en location sur Apparat</li>
    <li><strong>Mes réservations confirmés:</strong> l'ensemble des costumes que tu as confirmé pour une location à venir</li>
    <li><strong>A confirmer:</strong> les costumes en attente de validation (dépêche toi avant que ton loueur ne change d'avis) </li>
  </ol>


      <ul class="nav nav-tabs" id="myTab" role="tablist">
        <li class="nav-item">
          <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Mes costumes 👔</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Mes réservations confirmées ✓</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">A confirmer 👀</a>
        </li>
      </ul>
        <div class="tab-content" id="myTabContent">
          <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
      <div class="container">
          <div class="cards">
            <% current_user.costumes.each do |costume| %>
              <%= link_to costume_path(costume) do %>
                <% if costume.photos.attached? %>
                <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=cl_image_path costume.photos.first.key%>')">
                    <%=costume.name%>
                </div>
                  <% else %>
                <div>
                <%= %>
                <%= costume.name%>
                </div>
              <% end %>
                <% end %>
            <% end %>
          </div>
      </div>

          </div>
          <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">

          </div>
          <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">
          <div class="container">
              <% current_user.bookings.each do |booking| %>
                <div class="notification">
                <%= image_tag "https://www.vanityfair.fr/uploads/images/thumbs/201627/f0/marie_antoinette_slid_vf_5435.jpeg_north_375x310_transparent.jpg", class: "avatar-large"%>
                  <div class="notification-content">
                    <p> <small> <%= booking.costume.user.email %> </small></p>
                    <p> <%= booking.costume.user.email %> aimerait louer votre <%=booking.costume.name%> du <%=booking.beginning_date%> au <%=booking.ending_date%>.</p>
                    <p>Souhaitez-vous accepter ou refuser sa demande?</p>
                  </div>
                  <div class="notification-actions">
                  <%= link_to "✔️", "#", class: "btn btn-ghost" %>
                  <%= link_to "❌", booking_path(booking), method: :delete, data: {confirm: "Etes-vous sûr de vouloir supprimer cette réservation?"}, class: "btn btn-ghost" %>
                  </div>
                </div>
                <% end %>
          </div>

          </div>
      </div>
</div>
